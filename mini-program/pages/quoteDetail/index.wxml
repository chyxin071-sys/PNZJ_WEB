<view class="page-container" wx:if="{{!loading && quote}}">
  
  <view class="quote-header">
    <view class="flex justify-between items-center">
      <view class="quote-title">报价单 - {{quote.customer}}</view>
      <view class="status-badge">{{quote.status || '初步'}}</view>
    </view>
    <view class="address-text">{{quote.address}}</view>
  </view>

  <view class="summary-card">
    <view class="summary-row">
      <text class="label">预估总造价</text>
      <text class="value text-primary font-bold">¥{{quote.total || 0}}</text>
    </view>
    <view class="summary-row mt-2">
      <text class="label">最终成交价</text>
      <text class="value font-bold">¥{{quote.final || 0}}</text>
    </view>
  </view>

  <view class="items-section">
    <view class="section-title flex justify-between items-center" style="display: flex; justify-content: space-between; align-items: center;">
      <text>费用明细</text>
      <view class="btn-add-item" bindtap="goToSelectMaterial" style="font-size: 24rpx; color: var(--primary-600); padding: 6rpx 16rpx; border: 1rpx solid var(--primary-600); border-radius: 8rpx;">+ 添加明细</view>
    </view>
    
    <block wx:if="{{quote.items && quote.items.length > 0}}">
      <view class="item-card" wx:for="{{quote.items}}" wx:key="name">
        <view class="item-header">
          <text class="item-name">{{item.name}}</text>
          <text class="item-price">¥{{item.total || 0}}</text>
        </view>
        <view class="item-meta">
          <text>单价: ¥{{item.price}}</text>
        </view>
        <view style="display: flex; justify-content: space-between; align-items: center; margin-top: 16rpx;">
          <view class="stepper" style="display: flex; align-items: center; border: 1rpx solid #ddd; border-radius: 8rpx; overflow: hidden;">
            <view bindtap="updateQty" data-index="{{index}}" data-delta="-1" style="padding: 4rpx 20rpx; background: #f8f8f8; font-size: 28rpx; color: #666;">-</view>
            <view style="padding: 4rpx 20rpx; font-size: 26rpx; border-left: 1rpx solid #ddd; border-right: 1rpx solid #ddd;">{{item.quantity}}</view>
            <view bindtap="updateQty" data-index="{{index}}" data-delta="1" style="padding: 4rpx 20rpx; background: #f8f8f8; font-size: 28rpx; color: #666;">+</view>
          </view>
          <view style="font-size: 24rpx; color: #ef4444; padding: 6rpx 16rpx; border: 1rpx solid #fecaca; border-radius: 8rpx;" bindtap="deleteItem" data-index="{{index}}">删除</view>
        </view>
      </view>
    </block>
    <view class="empty-items" wx:else>
      <view class="icon-empty"></view>
      <text>暂无明细项</text>
      <text class="tip">点击上方按钮从材料库添加</text>
    </view>
  </view>
  
  <view class="bottom-tips">
    <text>提示：小程序端目前仅支持预览与生成报价单框架，完整的费用计算及材料添加请在PC端完成。</text>
  </view>

</view>